# ============================================================
# 多服务 Nginx 配置示例
# 放在 /etc/nginx/sites-available/ 下，每个服务一个文件
# 用 ln -s 启用
# ============================================================

# ---------- 服务 1: 采购管理系统 ----------
# 文件: /etc/nginx/sites-available/procurement.conf

server {
    listen 10001;
    server_name _;
    client_max_body_size 20m;

    location /assets/ {
        alias /opt/services/procurement/frontend/dist/assets/;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    location / {
        proxy_pass http://127.0.0.1:10001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_read_timeout 120s;
    }
}

# ---------- 服务 2: 示例（另一个 Python 服务）----------
# 文件: /etc/nginx/sites-available/wiki.conf

# server {
#     listen 10002;
#     server_name _;
#
#     location / {
#         proxy_pass http://127.0.0.1:10002;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     }
# }

# ---------- 服务 3: 示例（Node.js 服务）----------
# 文件: /etc/nginx/sites-available/monitor.conf

# server {
#     listen 10003;
#     server_name _;
#
#     location / {
#         proxy_pass http://127.0.0.1:10003;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#     }
# }

# ============================================================
# 启用方式
# ============================================================
# sudo ln -s /etc/nginx/sites-available/procurement.conf /etc/nginx/sites-enabled/
# sudo ln -s /etc/nginx/sites-available/wiki.conf /etc/nginx/sites-enabled/
# sudo nginx -t && sudo systemctl reload nginx
#
# 禁用方式
# sudo rm /etc/nginx/sites-enabled/wiki.conf
# sudo systemctl reload nginx
